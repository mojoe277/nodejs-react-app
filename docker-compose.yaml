# Use postgres/example user/password credentials
version: '3.9'

services:
    nodejs-react-app:  
      image: ${IMAGE}
      PORTS:
      - 8080:8080
    db:
      image: postgres
      restart: always
      environment:
         POSTGRES_PASSWORD: example
    adminer:
      image: adminer
      restart: always
      ports:
      - 5432:5432